# OmniCompost Site — Changelog

_Last updated: 2026-06-04_1530._

## [1.10] — 2026-06-04_1530

### Reverted
- **"The bench & community" section pulled from `learn.html`.** A proposed §V
  was added to `learn.html` — an "Ask the bench" email-intake block plus a
  "where owners trade notes" three-card row (vermicomposting subreddit / Field
  Notes reply thread / direct bench), framed as a deliberate decision *not* to
  run a brand forum (the dead-forum risk). On review it read as too much for the
  page, so it was backed out and `learn.html` restored to its four-section
  baseline (I. loop, II. feeding, III. one-page guides, IV. questions).
- **Archived, not lost.** The full with-section version is preserved at
  `archive/learn_with-bench-community_2026-06-04.html` in case the community
  idea is revived later (e.g. as a standalone SEO/support landing page). A
  standalone `community.html` draft explored during this work was also removed.

All notable changes to the OmniCompost website and mockups. Timestamps are
absolute, local time, in the project's `YYYY-MM-DD_HHMM` living-doc format.
Entries before **[1.9]** were recorded with date precision only (no time was
captured at the time); they are left as dates rather than back-filled with
invented times.

## [1.9] — 2026-06-01_2140

### Added
- **Tier favicons promoted into the brand system.** `favicon_premium` (sage,
  sharp square — the Premium tier's editorial signal), `favicon_family` (green,
  rounded) and `favicon_kids` (sky, rounded) are now generated by
  `omnicompost_brand/build_assets.py` as SVG masters + PNG ladders
  (16/32/48/180/192/512) + PDFs, and indexed in `assets/manifest.json`
  (56 → 80 files). Documented in `logo_usage_guide.html` as new **§3a — Tier
  favicons (digital)** plus a coverage-matrix row and a techniques-table row: the
  colour-chip favicons are the on-screen browser-tab / PWA app-icon expression,
  while the universal monogram stays the mark for foil/emboss/one-ink print.
- **Kids favicon reworked** — was dark ink on pale sky (washed out at 16px, the
  ground dominated and the strokes dissolved); now paper-on-saturated-blue
  (`#1690CC`), matching the family chip's geometry. Function (tab legibility)
  prioritised over monogram fidelity per brand-owner direction.

### Changed
- **`MAINSTREAM-PLAN.md` homepage direction decided** (new §4a): family-c "The
  Marketplace" (9.1) is the homepage; family-b "The Quick Start" (8.7) is the
  paid-traffic landing page; build order (§8) updated accordingly.
- Added `CONVENTIONS-ALIGNMENT.md` (Premium vs Family against 2026 conventions)
  and `scraps/tier-favicons/` (review + preview of the favicon set).

### Notes
- The **Family/Mainstream site** now has its **own changelog** at
  `mockups/omnicompost-family-site/CHANGELOG.md` — that site's build history lives
  there, not here.

## [1.8] — 2026-05-31

### Added
- **`gallery.html`** — a standalone editorial gallery built on the `learn.html`
  shell (same tokens/header/footer/cart). Three grouped masonry grids — **The
  harvest** (produce), **The garden** (soil/tools/flowers), **The pollinators**
  (bees/butterfly) — 23 figures with hover captions in Premium voice. Wired into
  the primary nav site-wide (index, shop, learn, newsletter, 404, product pages,
  Field Notes) and the homepage footer.
- **29 Premium images promoted** from `scraps/img-manual-pre-vetting/premium/`
  into `assets/img/` (slugged, resized ≤1600px). Feeds the new gallery, the
  homepage `#garden` band, and future PDP seconds / Field Notes. Standard Unsplash
  License only — the Unsplash+ (`premium_photo-*`) picks were left in scraps
  pending license confirmation.
- **Homepage nav dropdowns** — "Shop", "How it works", and "Gallery" are now
  hover/focus dropdowns offering both *jump to the on-page section* and *go to the
  full page* (`#shop`/shop.html, `#how`/learn.html, `#garden`/gallery.html),
  resolving the "subsection vs full page" ambiguity. CSS-only, keyboard-focusable.
- Homepage `#garden` ("What it gives back") band refreshed with two new stills
  (`rainbow-carrots-tray`, `bee-lavender-macro`) and a **"View the full gallery"**
  CTA to `gallery.html`.

### Fixed
- Stale homepage footer link "From counter to garden" → now **"Gallery"** →
  `gallery.html` (the §IV section it pointed to was removed in 1.7.1).

## [1.7.1] — 2026-05-30

### Changed
- **Homepage nav now scrolls within the page** — header "Shop" → `#shop` and
  "How it works" → `#how` (matching the other anchor links), instead of jumping to
  the full `shop.html` / `learn.html`. The path to the full pages is the in-body
  CTA at the end of each section (below) and the footer.
- **Removed the redundant homepage §IV "From counter to garden"** — its
  Tools/Operations/Tips zones overlapped §III "How it works" and are now fully
  carried by `learn.html`. Sections renumbered (gallery→IV, voices→V, questions→VI);
  the `#garden` nav anchor moved onto the gallery ("What it gives back"), which is
  the garden-output section. Section archived in source history if needed.

### Added
- **In-body links from the homepage to the full pages** — the shop section now
  ends with a **"View the full shop"** button to `shop.html`, and the on-page
  "How it works" summary (§III) ends with a **"Read the full guide"** button to
  `learn.html`. (Header nav + footer already linked both; these mirror the
  pattern the earlier scrap mockups used — a CTA at the end of each teaser
  section, not just in the chrome.)
- **`hands-cupped-seedling.jpg`** — one Premium keeper from a targeted batch-4
  image-sourcing pass (cupped hands + soil + maple seedling; warm-muted, no face).
  A planting-beat alternate, available for the learn page / future Field Notes.

### Notes
- The batch-4 pass otherwise confirmed the standing finding: Premium
  feeding/pouring action and Mainstream-Family people-present shots are not
  free-stock-solvable (Unsplash+ paywalled or faces/cold tone). The digester
  product, feeding action, and Kids set remain brand-shoot / commission blocked.
  See `scraps/kitchen-guide-image-candidates/image_coverage_2026-05-27.md`.

## [1.7] — 2026-05-30

### Added
- **`learn.html`** — a full "How it works" page: a 4-step detailed method, a
  feed-freely / moderation / keep-out table, a **one-page-guides section with QR
  placeholders** (Kitchen / Lawn & Garden / Troubleshooting cards — wired for
  future QR→PDF links), and a **large categorized FAQ** (14 Q&A across Setup,
  Feeding, Tea & Harvest, Travel/Seasons/Delivery). Premium voice, American
  English. Nav "How it works" now points here site-wide (index, shop, newsletter,
  404, product pages, Field Notes).
- **Open-graph / social cards** — `assets/img/og-card.jpg` (1200×630 crop of
  `compost-in-hands`) plus `og:*` + `twitter:card` meta on index, shop,
  newsletter, 404, learn, and every product page (PDPs use their own product
  image as `og:image`).
- **Second product images (PDP)** — optional `image2`/`alt2` in `products.json`;
  `build.py` renders a thumbnail strip with click-to-swap on the detail page.
  Added to 5 SKUs: The Digester, Tea Watering Can, Harvest Trowel, Seedling Pots,
  The Whole Loop.
- **Two images promoted from the candidate pool** into `assets/img/`:
  `watering-can-garden.jpg` and `tools-wood-door.jpg` (vetted against the Premium
  register; the four worm/soil `_img_candidates` were rejected as off-register).

### Changed
- **American English site-wide** — converted British spellings/vocabulary in all
  customer-facing copy (`odour→odor`, `centre→center`/`centrepiece→centerpiece`,
  `litre→liter`, `colour/labour/behaviour/neighbour`, and `fortnight` →
  `two-week`/`two weeks`/`twice-monthly`). Applied to index, shop, newsletter,
  404, `products.json` (→ rebuilt product pages), and all three Field Notes.
  `cart.js` had none.

## [1.6] — 2026-05-30

### Changed
- **Homepage copy refinement (AI-tell pass)** — `index.html` hero lede and the
  "quiet partner" featured block rewritten to remove machine-writing tells while
  keeping the Premium voice: the section-after-section em-dash pivot down the
  landing page was broken up (hero lede now reads as two clean sentences), and a
  filler triplet ("keep to themselves, ask for nothing, and never need turning")
  was trimmed to one real clause. Two lateral em-dash→colon swaps trialled in a
  draft were **rejected** for trading down on editorial warmth — voice beats
  detection-cleanliness.
- Whole-site copy reviewed against the same checklist. `shop.html`,
  `newsletter.html`, `notes/*.html`, and the `products.json` descriptions were
  **left unchanged** — their em dashes are varied (not a within-page tic), and
  product pages are viewed one at a time, so the repeated `desc` opening is not
  experienced as a pattern. Editing them would have been over-correction.

### Added
- **`404.html`** — branded not-found page (Premium tokens, header + footer,
  line-drawn worm, links back to the digester and the shop, `noindex`). Reuses
  `cart.js` so the cart count carries. Host/CDN should map the 404 route to it.
- **`human-writing` skill** (`.claude/skills/human-writing/`) — a final-pass
  checklist for stripping AI-writing tells from human-facing prose, with a
  "don't over-correct / keep the good parts of both drafts" rule and an
  audience-tier caveat (the Premium calibration does not transfer to Family/Kids).
- **`AI testing/` folder** (in the site) — effectiveness review of the skill run
  against the live homepage; the basis for the edits above.

### Archived
- `archive/index_v0.9_2026-05-30.html` (pre-edit homepage) and
  `archive/site_snapshot_2026-05-30_1910/` (full source snapshot of the live
  site: pages, `cart.js`, `products.json`, `build.py`, `product/`, `notes/` —
  `assets/` excluded as unchanged and heavy).

> Note: v1.5 (the **Mini Digester** $245 entry SKU, per `ROADMAP.md`) shipped to
> `products.json` without its own changelog entry; recorded here for continuity.

## [1.4] — 2026-05-30

### Added
- **Live inventory** — `stock` per product in `products.json`; `build.py` renders
  In stock / "Low stock — N left" / Sold out states and disables sold-out buttons;
  `cart.js` clamps quantity to `data-s`. Bedding Bricks ships sold-out (demo).
- **Orange County delivery gate** — `cart.js` injects a postcode field into the
  cart drawer on every page and locks Checkout until a valid OC code is entered
  (ranges `90620–90899`, `92600–92899`), persisted across pages.
- **ESP-ready newsletter** — `newsletter.html` form wired to a configurable
  `ESP_ENDPOINT` (honeypot + loading/success/error states, graceful demo fallback,
  no API keys in-file).
- **`guides/` folder** — reviewed v2 rewrites of the process/dev guides
  (model-selection, token-levers, serving-locally, build-strategy), the
  print-proofing standard, and review layers for the two skills, with an index.

### Changed
- **Field Notes expanded ~2.5×** — `notes/april-2026.html`, `march-2026.html`,
  `february-2026.html` lengthened (3 → 7–8 paragraphs) in Premium voice, each with
  two photographs from the vetted set.
- Home Section V gallery rebalanced to 7 images (clean 2-row block).
- ROADMAP marked items 3–5 (ESP, inventory, delivery gate) done; added Tracks C
  (coupons), D (payment methods), E (skills / CLAUDE.md).
- Site docs (this file, ROADMAP, REVIEW, SUMMARY) now carry in-file timestamps.

### Verified
- Shop/PDP stock states render correctly (in/low/sold-out); sold-out button
  disabled; cart clamps to stock; OC gate locks/unlocks checkout by postcode;
  newsletter honeypot + states present; all 6 new Field Notes images 200; no
  console errors (Claude Preview MCP, port 8099).

## [1.3] — 2026-05-29

### Added
- **Per-product detail pages** — `product/<slug>.html` for all 9 SKUs, generated by
  `build.py` from `products.json` (Premium PDP: image, rating, price, description, spec
  list, add-to-cart, breadcrumb). Enriched `products.json` with `desc` + `details` per item.
- **Field Notes archive issues** — real article pages under `notes/`
  (`april-2026.html`, `march-2026.html`, `february-2026.html`); the newsletter archive
  "Read the opening" links now resolve.

### Changed
- Shop card titles (`index.html`, `shop.html`) now link to their product page.
- **Cart accessibility pass** — drawer is `role="dialog" aria-modal` with a focus-trap and
  Esc-to-close; cart button toggles `aria-expanded`; an `aria-live` region announces the
  cart count. Applied across `index.html`, `shop.html`, `newsletter.html`, product, and
  notes pages via the shared `cart.js`.

### Verified
- All `product/` and `notes/` routes 200; product PDP renders (image, 4 specs, price);
  9 card titles link to product pages; add-to-cart opens drawer + sets `aria-expanded`,
  Esc closes + restores focus, live region announces; no console errors (preview, port 8099).

## [1.2] — 2026-05-29

### Added
- **`products.json`** — single source of truth for the catalog (9 SKUs: store metadata +
  per-product id, slug, name, category, price, featured, badge, cta, image, alt, rating, blurb).
- **`build.py`** — regenerates the product grid (the `BUILD:SHOP` marker region) in
  `index.html` and `shop.html` in place from `products.json`, emitting the existing Premium
  `.pcard` markup so the design is unchanged. Re-run with `C:\anaconda\python.exe build.py`.

### Changed
- `index.html` and `shop.html` product grids are now generated, not hand-edited — wrapped in
  `<!-- BUILD:SHOP -->` … `<!-- /BUILD:SHOP -->` markers.

### Verified
- `build.py` rebuilds both pages (9 products each); 9 `.pcard`s render, digester stays featured
  2×2, filters work (garden → 3), add-to-cart adds, cart count persists index → shop, no console
  errors (preview, port 8099).

## [1.1] — 2026-05-29

### Added
- **Cart persistence** — cart now saved to `localStorage` (`oc_cart`) and synced across
  pages and browser tabs (`storage` event).
- Shared **`cart.js`** — single source of truth for add-to-cart, drawer, qty, filters,
  and persistence. `index.html` refactored to use it (inline cart removed).
- **`shop.html`** — dedicated full-catalog page (9 SKUs, filters, cart drawer).
- **`newsletter.html`** — Field Notes sign-up page with archive teaser and client-side
  form confirmation.

### Changed
- Header + footer nav now link to `shop.html` and `newsletter.html`.

### Verified
- Cart count + contents persist across index → shop → newsletter; filters and the
  newsletter form work; no console errors (preview, port 8099).

### Roadmap
- Added Track A (commerce build): Snipcart checkout, per-product pages, `products.json`
  single-source, ESP for newsletter, inventory, delivery gate, accessibility pass.

## [1.0] — 2026-05-29

### Added
- Full production main site ([index.html](index.html)): Premium landing shell + storefront
  in one page — 9 products (digester featured 2×2), filter chips, working slide-in cart
  (live count, subtotal, qty +/−, remove, duplicate-merge).
- Multi-SKU shop + working cart added to every mockup: premium-a (Editorial),
  premium-b (Gallery), family (Mainstream Family). premium-c (Boutique) already had one.
- Kids mockup ([mockups/kids/index.html](mockups/kids/index.html)) — Worm Crew, engagement-only (no cart by design).
- Mockups picker page ([mockups/index.html](mockups/index.html)).
- [REVIEW.md](REVIEW.md) — overall review of the main website (9.6/10).
- [mockups/RATINGS.md](mockups/RATINGS.md) — ratings for all mockups + main site.

### Changed
- New Premium favicon (sage "A"-monogram `favicon.svg`) applied to all Premium surfaces:
  main site, premium-a/b/c, and the picker. Family and Kids keep their own marks by design.
- Reworked all mockups + main page around a 7–10 SKU consumables store while keeping the
  digester featured.
- Renumbered main-site Roman-numeral sections to fit the new Shop section (II).
- Main-site digester CTA changed from a Reserve `mailto:` to a working Add-to-cart.
- RATINGS.md rewritten for the finished state (was a first pass that flagged missing shops).

### Fixed
- Kids/Family: swapped 🪱 (renders blank on Windows 10) → 🐛.
- Kids hero sticker given a white card backing for the sticker-book aesthetic.

### Verified
- Main site, premium-a, premium-b, family carts all add correctly; no console errors;
  no broken images; responsive shop grid confirmed (preview, port 8099).

### Known stubs (backend, not design)
- Checkout is a demo alert; no per-product pages; no live inventory; cart is session-only;
  newsletter form is inert.
